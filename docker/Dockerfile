#ARG BASE_IMAGE
#FROM ${BASE_IMAGE}
FROM alpine
RUN apk add openjdk8-jre
ENV SPRING_CLOUD_CONFIG_ENABLED=true \
    SPRING_CLOUD_DISCOVERY_ENABLED=true
WORKDIR $HOME
EXPOSE 8080
COPY run.sh $HOME/
COPY *.jar $HOME/
CMD $HOME/run.sh
